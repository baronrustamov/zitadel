# All possible options and their defaults: https://github.com/zitadel/zitadel/blob/main/cmd/defaults.yaml
Log:
  Level: 'debug'
# Make ZITADEL accessible over HTTP, not HTTPS
ExternalSecure: true
ExternalDomain: id.prtl.cc
ExternalPort: 443

# CREATE DATABASE prtlztd;
# CREATE USER prtlvld WITH SUPERUSER PASSWORD 'paswztdl17d2tlpr';
# GRANT CONNECT, CREATE ON DATABASE prtlztd TO prtlvld;

Database:
  postgres:
    Host: 95.217.79.178 # db.prtl.cc
    Port: 5432
    Database: zitadel
    MaxOpenConns: 25
    MaxConnLifetime: 1h
    MaxConnIdleTime: 5m
    Options:
    User:
      Username: zitadel
      Password: paswztdl17d2tlpr
      SSL:
        Mode: disable
        RootCert:
        Cert:
        Key:
    Admin:
      Username: zitadel
      Password: paswztdl17d2tlpr
      SSL:
        Mode: disable
        RootCert:
        Cert:
        Key:
